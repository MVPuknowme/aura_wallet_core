# Deployment review

The deployment review view should show generated manifests, Helm values, L2 intent payloads, and a final operator command list. Review screens must redact private keys, mnemonics, API keys, passwords, and bearer tokens.
